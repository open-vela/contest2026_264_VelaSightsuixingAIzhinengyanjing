# ============================================================
# 本仓生效中的 .gitignore（原为组委会给的示例，已按本项目实际情况增补）
#
# 主要用来忽略编译产物，避免把它们误提交到参赛仓。
#
# 注意：logs/ 下最终导出的 AI Coding 日志必须提交，不要忽略！
#      assets/ 下的图片与视频是作品证据，也必须提交，所以这里**没有**
#      按扩展名忽略 *.jpg / *.png / *.mp4。
# ============================================================

# Build artifacts
build/
out/
*.o
*.a
*.elf
*.hex
*.map
.depend
*.dep
BTdocs/
notes/
music.mp3
music.flac

# Vendor confidential documents (not for open-source distribution)
*Datasheet*.pdf
board/beken/chips/bk7258/sim_tests

# Local scratch notes, not for submission
docs/local/

# ============================================================
# Code map (codemap/ is an Obsidian vault)
#
# 生成产物 codemap/generated/ 是**提交**的：它是给人和 AI 读的地图，
# 而且提交它才能靠 git diff 看出代码变化带来的地图变化。
# 这里忽略的只有本机私有的编辑器/缓存产物。
# 校验是否过期：sh codemap/_tools/check_codemap.sh
# ============================================================
codemap/.obsidian/
codemap/.trash/
codemap/.smart-env/
codemap/_tools/__pycache__/

# 代码地图产物（UX 测试期，仅本地）
codemap/

# ============================================================
# 以下为按本项目实际情况增补
# ============================================================

# 密钥：绝不能提交
#
# packages/ai_agent/include/agent_secrets.h 是填了 MiMo API key 的那份，
# 模板在 board/.../ai_agent/agent_secrets.h.example（模板要提交，填了 key 的
# 不能）。上游公共仓并没有忽略这个文件名，所以在本仓也兜一道：万一有人把它
# 复制进来调试，也不会被 git add 带走。
agent_secrets.h
.env
*.pem
*.key

# 上板抓取与还原过程中的中间产物
#
# 文档里的流程都写去 /tmp，但 serial_cmd.sh -o 可以指到任何地方；这几类一旦
# 落在仓内都是几十 KB 到几百 KB 的一次性数据，没有复现价值（复现命令在
# 摄像头取帧接口与显示参数.md 第六节）。
# 注意不要在这里加 *.jpg/*.png/*.mp4：assets/ 下的图片和视频是要提交的证据。
*.log
*.yuv
*.raw
*.bin
*.rbl

# Python 工具的缓存（tools/ 下有脚本，在仓内直接跑会生成）
__pycache__/
*.pyc

# 编辑器与操作系统的临时文件
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
